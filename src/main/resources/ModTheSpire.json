{
  "modid": "${project.artifactId}",
  "name": "${project.name}",
  "author_list": ["squeeny"],
  "description": "Unlocks A20 for all characters, modded or otherwise.",
  "version": "${project.version}",
  "sts_version": "${SlayTheSpire.version}",
  "mts_version": "${ModTheSpire.version}",
  "dependencies": ["basemod", "stslib"],
  "update_json": ""
}

